/**
 * Top-level build file for ftc_app project.
 *
 * It is extraordinarily rare that you will ever need to edit this file.
 */

buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        // Note for FTC Teams: Do not modify this yourself.
        classpath 'com.android.tools.build:gradle:8.1.2'
    }
}

System.setProperty("user.dir", project.projectDir.toString())
System.println("Configuration Started: CenterStage")
System.println("********************************")
System.println("project dir=" + project.projectDir.toString())
System.println("project version=" + project.version.toString())
System.println("gradle version=" + gradle.gradleVersion)
System.println("root project=" + gradle.rootProject)
System.println("gradle java version=" + System.getProperty("java.version"))


// This is now required because aapt2 has to be downloaded from the
// google() repository beginning with version 3.2 of the Android Gradle Plugin
allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

repositories {
    mavenCentral()
}
